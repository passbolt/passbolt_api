<%
if (typeof cssClasses == 'undefined') {
    cssClasses = '';
}
%>

<li id="<%= id %>" <%= (el) -> el.data( itemClass.fullName, item ) %> class="<%= cssClasses %>">
    <div class="main-cell">
        <a href="#"><span><%= label %></span></a>
    </div>
</li>
