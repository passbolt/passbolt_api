<div class="resource">
	<h3><%= resource.name %></h3>
	<a href="#" class="dialog-close"><i class="icon close no-text"></i><span>close</span></a>

	<div class="detailed-information">
	<h4><%= __('Information') %></h4>
	<ul>
		<li class="modified">
			<span class="label"><%= __('Modified') %></span>
			<span class="value"><%= moment(resource.modified).fromNow() %></span>
		</li>
		<li class="modified-by">
			<span class="label"><%= __('Modified by') %></span>
			<span class="value"><%= resource.Modifier.username %></span>
		</li>
		<li class="expire">
			<span class="label"><%= __('Expire') %></span>
			<span class="value"><%= resource.expiry_date != null ? moment(resource.expiry_date).fromNow() : __('never') %></span>
		</li>
		<li class="strength">
			<span class="label"><%= __('Strength') %></span>
			<span>
				<%= secretStrength ? secretStrength.label : '-' %>
			</span>
			</li>
	</ul>
	</div>

	<div class="description">
		<h4><%= __('Description') %></h4>
		<a href="#editdesc" class="section-action todo"><i class="icon edit no-text"></i><span>edit</span></a>
		<p><%= resource.description %></p>
	</div>

	<!-- Tags management -->
	<div class="tags clearfix" id="js_rs_details_tags"></div>

	<!-- Comments management -->
	<div class="comments clearfix" id="js_rs_details_comments"></div>
</div>

