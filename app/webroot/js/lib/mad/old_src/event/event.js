steal(
	'can/construct'
).then(function () {

	// can.Construct('mad.event', /** @static */ {
// 
		// /**
		 // * Trigger an event on a target instance
		 // * @param {String} eventName Event name
		 // * @param {Array} eventData (Optional) Parameters to send with the event 
		 // * @return {void}
		 // */
		// 'trigger': function (instance, evtName, data) {
			// // trigger the event on the controller element
			// instance.element.trigger(evtName, data);
			// // trigger the event prefixed by the instance controller name on the controller element
			// instance.element.trigger(instance.getAlias('under') + '.' + evtName, data);
			// // trigger the event prefixed by the instance controller id on the controller element
			// instance.element.trigger(instance.getId() + '.' + evtName, data);
		// }
// 
	// },  /** @prototype */ {});

});