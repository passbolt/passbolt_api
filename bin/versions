#!/usr/bin/env bash

set -euo pipefail

DIR=$(dirname "$(readlink -f "$0")")

"$DIR"/cake passbolt version
uname -a
php --version | grep -m1 ""
mysql --version
gpg --version | grep -m2 ""
composer --version -n
