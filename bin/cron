#!/usr/bin/env bash
DIR="$(cd "$(dirname "$0")" && pwd)"
if [[ -z "$PASSBOLT_PLUGINS_EMAIL_DIGEST_ENABLED" ||  "$PASSBOLT_PLUGINS_EMAIL_DIGEST_ENABLED" = 0 ]] ; then
    $DIR/cake EmailQueue.sender
else
    $DIR/cake Passbolt/EmailDigest.sender
fi
