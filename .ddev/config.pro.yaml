# this config file inherits all the settings declared in config.yaml, here are only overrides

php_version: "8.3" # passbolt-ce supports 8.4 but the LDAP plugin in passbolt-pro triggers warnings in 8.4 due to a composer module

